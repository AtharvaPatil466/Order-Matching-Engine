\* TLC config for EngineConsumer.tla.
\* Default model: 2 producers × 2 items each, abstract queue (sequence).
\* Verifies the wake-up protocol used by MatchingEngine::workerLoop.

SPECIFICATION Spec

INVARIANTS
    TypeOK
    NoSpuriousItems
    NoDuplicates

PROPERTIES
    EventuallyDrained

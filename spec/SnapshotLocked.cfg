\* TLC config for SnapshotLocked.tla.
\* Verifies the strong consistency properties that were COUNTEREXAMPLES
\* in the unlocked spec. With bookLock_ held by the reader for the
\* duration of its 2-step snapshot, both
\* SnapshotIsPointInTime and NoDoubleSidedSnapshot must hold for every
\* reachable state.

SPECIFICATION Spec

INVARIANTS
    TypeOK
    SnapshotIsPointInTime
    NoDoubleSidedSnapshot
    ReaderConsistency

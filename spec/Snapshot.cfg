\* TLC config for Snapshot.tla.
\* Verifies the properties that DO hold under lockless snapshot reads.
\* For the deliberate-violation properties (SnapshotIsPointInTime and
\* NoDoubleSidedSnapshot), edit the INVARIANTS section to include them
\* one at a time and re-run; TLC will produce a counterexample trace
\* showing the torn-read scenario.

SPECIFICATION Spec

INVARIANTS
    TypeOK
    SnapshotEntriesAreReal

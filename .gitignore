build/
build-*/
build_*/
bin/

# PGO profile data — never commit; regenerated by scripts/pgo_build.sh
*.profraw
*.profdata

# aws_benchmark.sh run logs
aws-benchmark-*.log

# benchmark-gate.yml A/B run output — root only, not bcs_research/results/
# (head-*.json / base-*.json). The leading slash is load-bearing: unanchored
# `results/` matches at any depth and silently swallows the BCS research tree.
/results/

.DS_Store
*.o
*.a
*.dylib
*.so

# AI Agents & Assistants
.claude/
CLAUDE.md
AGENTS.md
GEMINI.md
.cursor/
.cursorrules
.windsurfrules
.kiro/
.mcp.json
.opencode.json

# Generated Artifacts

# Personal / Debug
tsan_out*.txt

# IDEs & Tools
.idea/
.clangd/
.vscode/
.code-review-graph/
.antigravitycli/

# Python (chaos tests / tooling)
.venv/
__pycache__/
*.pyc

# Generated knowledge-graph cache (regenerable via /graphify)
graphify-out/
